name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Pull a JavaFX JDK
      run: curl -O "https://download.java.net/java/GA/jdk12/33/GPL/openjdk-12_windows-x64_bin.zip"
    - name: List directory contnts
      run: ls -la
    - uses: actions/checkout@v1- run: curl -O "https://download.java.net/java/GA/jdk12/33/GPL/openjdk-12_windows-x64_bin.zip"
    - name: Set Java
      uses: actions/setup-java@master
      with:
        version: 12
        architecture: x64
        jdkFile: ./openjdk-12_windows-x64_bin.zip
    - name: Build with Maven
      run: mvn package --file pom.xml
