name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - run: ssh -A vps03.hoblox.nl -- 'cd /var/www/webblocks.nl/blogsfeed/web && git pull && composer install -o --no-dev'
