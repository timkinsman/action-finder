name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Copy privkey
      env:
        PRIVKEY: ${{ secrets.PRIVKEY }}
      run: echo "$PRIVKEY" > ~/privkey && chmod 0600 ~/privkey
    - name: Deploy to hoblox
      env:
        SSH_HOST: ${{ secrets.SSH_HOST }}
      run: ssh -i ~/privkey -o "StrictHostKeyChecking=no" "$SSH_HOST" -- 'cdbf && git pull && composer install -o --no-dev'
